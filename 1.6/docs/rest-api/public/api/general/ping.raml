
get:
  description: Ping this Marathon instance.
  responses:
    200:
      description: Every ping is answered with a pong.
      body:
        text/plain:
          example: pong
        application/json:
          type: string
          example: "pong"
