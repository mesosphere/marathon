#%RAML 1.0 DataType
type: object
(scalaType): Constraint
properties:
  fieldName:
    type: string
  operator:
    (scalaType): ConstraintOperator
    type: string
    enum: [UNIQUE, CLUSTER, GROUP_BY, LIKE, UNLIKE, MAX_PER]
  value?: string
